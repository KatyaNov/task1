Модель была построена на основе предобученной ResNet-50 и дообучалась в течение 10 эпох с функцией потерь «кросс-энтропия» и оптимизатором Adam. Качество модели на тестовой выборке составило:

Дополнительно реализованные функции: 
  (1) валидация модели на части обучающей выборки;
  (3) автоматическое сохранение модели при обучении;
  (5) вывод различных показателей в процессе обучения (например, значение функции потерь на каждой эпохе);
  (6) построение графиков, визуализирующих процесс обучения (например, график зависимости функции потерь от номера эпохи обучения);
  (11) аугментация;
